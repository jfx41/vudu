TODO:

  1. Add: optarg support:
  1.1. --help
  1.2. --catalog (Movies.csv)
  1.3. --cache-dir (entry_dir; Cached results, picks up where you left off)
  1.4. --init (Creates ~/.vudu/cache/, ~/.vudu.conf)
  1.5. --percentage (If not a perfect match, only print results >= percentage)

  2. The results output is lame.  You should probably fix that.

  3. Make the module/class more usable.

  4. Documentation.  Right .. this is going to happen. /=

